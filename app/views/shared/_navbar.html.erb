<nav class="navbar sticky-top navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand mx-5" href="/">Kickslease</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav me-auto pt-8">
      <li class="nav-item active ">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li>
        <a class="nav-link" href="/shoes/new">Add Kicks</a>
      </li>
       <li>
        <a class="nav-link" href="/bookings">Bookings</a>
      </li>
      <% if !current_user.nil? %>
        <li>
          <%= link_to "My Kicks", user_path(current_user), class:"nav-link" %>
        </li>
      <% end %>
    </ul>
    <%= form_with url: root_path, method: :get, class: "form-inline my-2 my-lg-0 row pr-8 mx-5 d-flex" do %>
    <%= text_field_tag :query,
    params[:query],
    class: "form-control mr-sm-2",
    placeholder: "Find kicks"
    %>
    <%# <%= submit_tag "Search", class: "bbutton" %>
    <% end %>
    <%# <form class="form-inline my-2 my-lg-0 row pr-8 mx-5 d-flex">
      <input name= "query" class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
    </form> %>
    <% if user_signed_in? %>
      <p><%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class:"text-decoration-none px-4 text-secondary"%></p>
      <% else %>
      <p><%= link_to "Login", new_user_session_path, class:"text-decoration-none px-4 text-secondary"%></p>
    <% end %>
  </div>
</nav>
